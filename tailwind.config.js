/** @type {import('tailwindcss').Config} */
export const content = ['./index.html', './src/**/*.{js,ts,jsx,tsx}'];
export const theme = {
  
  extend: {
    colors: {
      primary: '#ffffff', // Customize if needed
      gold: '#FFD700',
    },
  },
};
export const plugins = [];
